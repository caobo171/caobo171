# Database (optional for now — site runs without it)
# DATABASE_URL=postgres://user:password@localhost:5432/caobo171

# Example with Docker Compose in this folder:
# docker compose up -d
# DATABASE_URL=postgres://caobo:caobo@localhost:5432/caobo171
